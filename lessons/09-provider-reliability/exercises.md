# 第 9 课练习

1. 测试带 ```json code fence 的输出。
2. 测试缺少 `answer` 字段。
3. 测试 JSON 合法但业务字段为空。
4. 注入一次 retryable 错误，观察重试次数。
5. 注入 configuration 错误，确认不重试。
6. 为超时错误增加独立错误码。

## 验收

- [ ] 能区分解析、类型校验和业务校验。
- [ ] 能区分可重试和不可重试错误。
- [ ] 能解释为什么模型输出不能直接信任。
