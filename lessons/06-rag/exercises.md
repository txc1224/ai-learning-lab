# 第 6 课练习

1. 将最大 chunk 长度从 240 改为 80，观察分片数量。
2. 添加一个 `source` 不同的文档，观察引用。
3. 测试空文档和无命中查询。
4. 修改关键词评分，避免同一关键词重复计分。
5. 写一个假的 `Embedder` 接口，不实现真实向量模型。
6. 把检索上下文拼进 MockProvider 的 system message。

## 验收

- [ ] 能解释切分为什么影响召回。
- [ ] 能区分关键词检索和向量检索。
- [ ] 能看到命中分片和引用来源。
- [ ] 能单独评估检索结果，不把生成答案当成召回指标。
