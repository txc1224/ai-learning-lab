# 第 10 课练习

1. 为状态 store 增加 `fail` 和 `cancel` 方法。
2. 测试相同幂等键不会创建第二条记录。
3. 测试事件序号不会倒退。
4. 设计 PostgreSQL 的 `assistant_message` 增量更新 SQL。
5. 解释为什么数据库写入重试需要幂等键。

## 验收

- [ ] 能画出流状态机。
- [ ] 能解释幂等键。
- [ ] 能区分客户端断开和 Provider 失败。
